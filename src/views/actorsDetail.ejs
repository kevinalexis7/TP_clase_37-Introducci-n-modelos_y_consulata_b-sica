<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= actor.name %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <a href="/"><img src="/img/logo-DH.png" alt="Logo Digital House"></a>
    </figure>    
    <h1><%= actor.first_name +' '+ actor.last_name %> </h1>
    <br>
    <p>NAME: <%= actor.first_name +' '+ actor.last_name %> </p>
    <p>RATING: <%= actor.rating %> </p>
    <p>FAVORITE MOVIE: <% if (!movie) { %> <p>No hay datos de película favorita</p>
    <% }else{ %>
        <a href="/movies/detail/<%= movie.id %>"><%= movie.title %></a>
    <% } %> </p>

</body>
</html>